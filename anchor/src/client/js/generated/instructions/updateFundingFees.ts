/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from 'gill';
import { PERP_NEXUS_PROGRAM_ADDRESS } from '../programs';
import {
  expectSome,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared';

export const UPDATE_FUNDING_FEES_DISCRIMINATOR = new Uint8Array([
  33, 65, 8, 5, 13, 128, 175, 104,
]);

export function getUpdateFundingFeesDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    UPDATE_FUNDING_FEES_DISCRIMINATOR
  );
}

export type UpdateFundingFeesInstruction<
  TProgram extends string = typeof PERP_NEXUS_PROGRAM_ADDRESS,
  TAccountCranker extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountTraderProfile extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends
    | string
    | AccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountCranker extends string
        ? WritableSignerAccount<TAccountCranker> &
            AccountSignerMeta<TAccountCranker>
        : TAccountCranker,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountTraderProfile extends string
        ? WritableAccount<TAccountTraderProfile>
        : TAccountTraderProfile,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type UpdateFundingFeesInstructionData = {
  discriminator: ReadonlyUint8Array;
  trader: Address;
  updatedFees: bigint;
};

export type UpdateFundingFeesInstructionDataArgs = {
  trader: Address;
  updatedFees: number | bigint;
};

export function getUpdateFundingFeesInstructionDataEncoder(): FixedSizeEncoder<UpdateFundingFeesInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['trader', getAddressEncoder()],
      ['updatedFees', getI64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: UPDATE_FUNDING_FEES_DISCRIMINATOR })
  );
}

export function getUpdateFundingFeesInstructionDataDecoder(): FixedSizeDecoder<UpdateFundingFeesInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['trader', getAddressDecoder()],
    ['updatedFees', getI64Decoder()],
  ]);
}

export function getUpdateFundingFeesInstructionDataCodec(): FixedSizeCodec<
  UpdateFundingFeesInstructionDataArgs,
  UpdateFundingFeesInstructionData
> {
  return combineCodec(
    getUpdateFundingFeesInstructionDataEncoder(),
    getUpdateFundingFeesInstructionDataDecoder()
  );
}

export type UpdateFundingFeesAsyncInput<
  TAccountCranker extends string = string,
  TAccountConfig extends string = string,
  TAccountTraderProfile extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  cranker: TransactionSigner<TAccountCranker>;
  config?: Address<TAccountConfig>;
  traderProfile?: Address<TAccountTraderProfile>;
  systemProgram?: Address<TAccountSystemProgram>;
  trader: UpdateFundingFeesInstructionDataArgs['trader'];
  updatedFees: UpdateFundingFeesInstructionDataArgs['updatedFees'];
};

export async function getUpdateFundingFeesInstructionAsync<
  TAccountCranker extends string,
  TAccountConfig extends string,
  TAccountTraderProfile extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof PERP_NEXUS_PROGRAM_ADDRESS,
>(
  input: UpdateFundingFeesAsyncInput<
    TAccountCranker,
    TAccountConfig,
    TAccountTraderProfile,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  UpdateFundingFeesInstruction<
    TProgramAddress,
    TAccountCranker,
    TAccountConfig,
    TAccountTraderProfile,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? PERP_NEXUS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    cranker: { value: input.cranker ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    traderProfile: { value: input.traderProfile ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([99, 111, 110, 102, 105, 103])),
      ],
    });
  }
  if (!accounts.traderProfile.value) {
    accounts.traderProfile.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([116, 114, 97, 100, 101, 114])),
        getAddressEncoder().encode(expectSome(args.trader)),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.cranker),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.traderProfile),
      getAccountMeta(accounts.systemProgram),
    ],
    programAddress,
    data: getUpdateFundingFeesInstructionDataEncoder().encode(
      args as UpdateFundingFeesInstructionDataArgs
    ),
  } as UpdateFundingFeesInstruction<
    TProgramAddress,
    TAccountCranker,
    TAccountConfig,
    TAccountTraderProfile,
    TAccountSystemProgram
  >;

  return instruction;
}

export type UpdateFundingFeesInput<
  TAccountCranker extends string = string,
  TAccountConfig extends string = string,
  TAccountTraderProfile extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  cranker: TransactionSigner<TAccountCranker>;
  config: Address<TAccountConfig>;
  traderProfile: Address<TAccountTraderProfile>;
  systemProgram?: Address<TAccountSystemProgram>;
  trader: UpdateFundingFeesInstructionDataArgs['trader'];
  updatedFees: UpdateFundingFeesInstructionDataArgs['updatedFees'];
};

export function getUpdateFundingFeesInstruction<
  TAccountCranker extends string,
  TAccountConfig extends string,
  TAccountTraderProfile extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof PERP_NEXUS_PROGRAM_ADDRESS,
>(
  input: UpdateFundingFeesInput<
    TAccountCranker,
    TAccountConfig,
    TAccountTraderProfile,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): UpdateFundingFeesInstruction<
  TProgramAddress,
  TAccountCranker,
  TAccountConfig,
  TAccountTraderProfile,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? PERP_NEXUS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    cranker: { value: input.cranker ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    traderProfile: { value: input.traderProfile ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.cranker),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.traderProfile),
      getAccountMeta(accounts.systemProgram),
    ],
    programAddress,
    data: getUpdateFundingFeesInstructionDataEncoder().encode(
      args as UpdateFundingFeesInstructionDataArgs
    ),
  } as UpdateFundingFeesInstruction<
    TProgramAddress,
    TAccountCranker,
    TAccountConfig,
    TAccountTraderProfile,
    TAccountSystemProgram
  >;

  return instruction;
}

export type ParsedUpdateFundingFeesInstruction<
  TProgram extends string = typeof PERP_NEXUS_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    cranker: TAccountMetas[0];
    config: TAccountMetas[1];
    traderProfile: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: UpdateFundingFeesInstructionData;
};

export function parseUpdateFundingFeesInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedUpdateFundingFeesInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      cranker: getNextAccount(),
      config: getNextAccount(),
      traderProfile: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getUpdateFundingFeesInstructionDataDecoder().decode(instruction.data),
  };
}
