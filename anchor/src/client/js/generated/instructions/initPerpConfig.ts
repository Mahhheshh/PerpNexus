/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from 'gill';
import { PERP_NEXUS_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const INIT_PERP_CONFIG_DISCRIMINATOR = new Uint8Array([
  243, 145, 211, 0, 200, 68, 99, 85,
]);

export function getInitPerpConfigDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    INIT_PERP_CONFIG_DISCRIMINATOR
  );
}

export type InitPerpConfigInstruction<
  TProgram extends string = typeof PERP_NEXUS_PROGRAM_ADDRESS,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountProtocolVault extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends
    | string
    | AccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAdmin extends string
        ? WritableSignerAccount<TAccountAdmin> &
            AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      TAccountConfig extends string
        ? WritableAccount<TAccountConfig>
        : TAccountConfig,
      TAccountProtocolVault extends string
        ? ReadonlyAccount<TAccountProtocolVault>
        : TAccountProtocolVault,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitPerpConfigInstructionData = {
  discriminator: ReadonlyUint8Array;
  cranker: Address;
  fees: number;
};

export type InitPerpConfigInstructionDataArgs = {
  cranker: Address;
  fees: number;
};

export function getInitPerpConfigInstructionDataEncoder(): FixedSizeEncoder<InitPerpConfigInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['cranker', getAddressEncoder()],
      ['fees', getU16Encoder()],
    ]),
    (value) => ({ ...value, discriminator: INIT_PERP_CONFIG_DISCRIMINATOR })
  );
}

export function getInitPerpConfigInstructionDataDecoder(): FixedSizeDecoder<InitPerpConfigInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['cranker', getAddressDecoder()],
    ['fees', getU16Decoder()],
  ]);
}

export function getInitPerpConfigInstructionDataCodec(): FixedSizeCodec<
  InitPerpConfigInstructionDataArgs,
  InitPerpConfigInstructionData
> {
  return combineCodec(
    getInitPerpConfigInstructionDataEncoder(),
    getInitPerpConfigInstructionDataDecoder()
  );
}

export type InitPerpConfigAsyncInput<
  TAccountAdmin extends string = string,
  TAccountConfig extends string = string,
  TAccountProtocolVault extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  config?: Address<TAccountConfig>;
  protocolVault?: Address<TAccountProtocolVault>;
  systemProgram?: Address<TAccountSystemProgram>;
  cranker: InitPerpConfigInstructionDataArgs['cranker'];
  fees: InitPerpConfigInstructionDataArgs['fees'];
};

export async function getInitPerpConfigInstructionAsync<
  TAccountAdmin extends string,
  TAccountConfig extends string,
  TAccountProtocolVault extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof PERP_NEXUS_PROGRAM_ADDRESS,
>(
  input: InitPerpConfigAsyncInput<
    TAccountAdmin,
    TAccountConfig,
    TAccountProtocolVault,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  InitPerpConfigInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountConfig,
    TAccountProtocolVault,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? PERP_NEXUS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: true },
    protocolVault: { value: input.protocolVault ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([99, 111, 110, 102, 105, 103])),
      ],
    });
  }
  if (!accounts.protocolVault.value) {
    accounts.protocolVault.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([118, 97, 117, 108, 116])),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.admin),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.protocolVault),
      getAccountMeta(accounts.systemProgram),
    ],
    programAddress,
    data: getInitPerpConfigInstructionDataEncoder().encode(
      args as InitPerpConfigInstructionDataArgs
    ),
  } as InitPerpConfigInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountConfig,
    TAccountProtocolVault,
    TAccountSystemProgram
  >;

  return instruction;
}

export type InitPerpConfigInput<
  TAccountAdmin extends string = string,
  TAccountConfig extends string = string,
  TAccountProtocolVault extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  config: Address<TAccountConfig>;
  protocolVault: Address<TAccountProtocolVault>;
  systemProgram?: Address<TAccountSystemProgram>;
  cranker: InitPerpConfigInstructionDataArgs['cranker'];
  fees: InitPerpConfigInstructionDataArgs['fees'];
};

export function getInitPerpConfigInstruction<
  TAccountAdmin extends string,
  TAccountConfig extends string,
  TAccountProtocolVault extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof PERP_NEXUS_PROGRAM_ADDRESS,
>(
  input: InitPerpConfigInput<
    TAccountAdmin,
    TAccountConfig,
    TAccountProtocolVault,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): InitPerpConfigInstruction<
  TProgramAddress,
  TAccountAdmin,
  TAccountConfig,
  TAccountProtocolVault,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? PERP_NEXUS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: true },
    protocolVault: { value: input.protocolVault ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.admin),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.protocolVault),
      getAccountMeta(accounts.systemProgram),
    ],
    programAddress,
    data: getInitPerpConfigInstructionDataEncoder().encode(
      args as InitPerpConfigInstructionDataArgs
    ),
  } as InitPerpConfigInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountConfig,
    TAccountProtocolVault,
    TAccountSystemProgram
  >;

  return instruction;
}

export type ParsedInitPerpConfigInstruction<
  TProgram extends string = typeof PERP_NEXUS_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    admin: TAccountMetas[0];
    config: TAccountMetas[1];
    protocolVault: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: InitPerpConfigInstructionData;
};

export function parseInitPerpConfigInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedInitPerpConfigInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      admin: getNextAccount(),
      config: getNextAccount(),
      protocolVault: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitPerpConfigInstructionDataDecoder().decode(instruction.data),
  };
}
