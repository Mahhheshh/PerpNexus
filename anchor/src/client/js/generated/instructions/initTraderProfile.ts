/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from 'gill';
import { PERP_NEXUS_PROGRAM_ADDRESS } from '../programs';
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared';

export const INIT_TRADER_PROFILE_DISCRIMINATOR = new Uint8Array([
  13, 6, 248, 22, 15, 213, 13, 110,
]);

export function getInitTraderProfileDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    INIT_TRADER_PROFILE_DISCRIMINATOR
  );
}

export type InitTraderProfileInstruction<
  TProgram extends string = typeof PERP_NEXUS_PROGRAM_ADDRESS,
  TAccountTrader extends string | AccountMeta<string> = string,
  TAccountTraderProfile extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends
    | string
    | AccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountTrader extends string
        ? WritableSignerAccount<TAccountTrader> &
            AccountSignerMeta<TAccountTrader>
        : TAccountTrader,
      TAccountTraderProfile extends string
        ? WritableAccount<TAccountTraderProfile>
        : TAccountTraderProfile,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitTraderProfileInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type InitTraderProfileInstructionDataArgs = {};

export function getInitTraderProfileInstructionDataEncoder(): FixedSizeEncoder<InitTraderProfileInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: INIT_TRADER_PROFILE_DISCRIMINATOR })
  );
}

export function getInitTraderProfileInstructionDataDecoder(): FixedSizeDecoder<InitTraderProfileInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getInitTraderProfileInstructionDataCodec(): FixedSizeCodec<
  InitTraderProfileInstructionDataArgs,
  InitTraderProfileInstructionData
> {
  return combineCodec(
    getInitTraderProfileInstructionDataEncoder(),
    getInitTraderProfileInstructionDataDecoder()
  );
}

export type InitTraderProfileAsyncInput<
  TAccountTrader extends string = string,
  TAccountTraderProfile extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  trader: TransactionSigner<TAccountTrader>;
  traderProfile?: Address<TAccountTraderProfile>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export async function getInitTraderProfileInstructionAsync<
  TAccountTrader extends string,
  TAccountTraderProfile extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof PERP_NEXUS_PROGRAM_ADDRESS,
>(
  input: InitTraderProfileAsyncInput<
    TAccountTrader,
    TAccountTraderProfile,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  InitTraderProfileInstruction<
    TProgramAddress,
    TAccountTrader,
    TAccountTraderProfile,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? PERP_NEXUS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    trader: { value: input.trader ?? null, isWritable: true },
    traderProfile: { value: input.traderProfile ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.traderProfile.value) {
    accounts.traderProfile.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([116, 114, 97, 100, 101, 114])),
        getAddressEncoder().encode(expectAddress(accounts.trader.value)),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.trader),
      getAccountMeta(accounts.traderProfile),
      getAccountMeta(accounts.systemProgram),
    ],
    programAddress,
    data: getInitTraderProfileInstructionDataEncoder().encode({}),
  } as InitTraderProfileInstruction<
    TProgramAddress,
    TAccountTrader,
    TAccountTraderProfile,
    TAccountSystemProgram
  >;

  return instruction;
}

export type InitTraderProfileInput<
  TAccountTrader extends string = string,
  TAccountTraderProfile extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  trader: TransactionSigner<TAccountTrader>;
  traderProfile: Address<TAccountTraderProfile>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getInitTraderProfileInstruction<
  TAccountTrader extends string,
  TAccountTraderProfile extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof PERP_NEXUS_PROGRAM_ADDRESS,
>(
  input: InitTraderProfileInput<
    TAccountTrader,
    TAccountTraderProfile,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): InitTraderProfileInstruction<
  TProgramAddress,
  TAccountTrader,
  TAccountTraderProfile,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? PERP_NEXUS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    trader: { value: input.trader ?? null, isWritable: true },
    traderProfile: { value: input.traderProfile ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.trader),
      getAccountMeta(accounts.traderProfile),
      getAccountMeta(accounts.systemProgram),
    ],
    programAddress,
    data: getInitTraderProfileInstructionDataEncoder().encode({}),
  } as InitTraderProfileInstruction<
    TProgramAddress,
    TAccountTrader,
    TAccountTraderProfile,
    TAccountSystemProgram
  >;

  return instruction;
}

export type ParsedInitTraderProfileInstruction<
  TProgram extends string = typeof PERP_NEXUS_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    trader: TAccountMetas[0];
    traderProfile: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: InitTraderProfileInstructionData;
};

export function parseInitTraderProfileInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedInitTraderProfileInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      trader: getNextAccount(),
      traderProfile: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitTraderProfileInstructionDataDecoder().decode(instruction.data),
  };
}
